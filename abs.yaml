build_parameters:
  - "--suppress-variables"
  - "--error-overlinking"
  - "--error-overdepending"
  - "--no-test"

upload_channels:
  - ad-testing/label/py315

channels:
  - ad-testing/label/py315
  # llvmlite py315 is on PR staging until review-gated promotion (U12: strip before merge).
  - https://staging.continuum.io/pbp/fs/llvmlite-feedstock/pr23/a9c5e46

build_env_vars:
  ANACONDA_ROCKET_ENABLE_PY315: True
